syntax = "proto3";

package protos;
option csharp_namespace = "NPitaya.Protos";
option go_package = "./protos";

message KickMsg {
  string userId = 1;
  map<string, string> metadata = 2; // 用户调用Kic()时的透传数据
}

message KickAnswer { bool kicked = 1; }
